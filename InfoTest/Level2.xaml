<Window x:Class="InfoTest.Level2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InfoTest"
        mc:Ignorable="d"
        Title="Level2" 
        
        WindowState="Maximized" 
        WindowStyle="None" 
        ResizeMode="NoResize" 
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="Geheimknopf" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                        BorderBrush="Transparent" 
                        BorderThickness="0">

                            <Border.Background>
                                <SolidColorBrush Color="Black"/>
                            </Border.Background>

                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">

                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" 
                                                    Storyboard.TargetProperty="Background.Color" 
                                                    To="DarkGreen" 
                                                    Duration="0:0:0.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>

                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" 
                                                    Storyboard.TargetProperty="Background.Color" 
                                                    To="Black" 
                                                    Duration="0:0:0.5" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">

        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Button Background="Black" Style="{StaticResource Geheimknopf}" Grid.Row="0" Grid.Column="0"  x:Name="r1" Visibility="Hidden" Click="r_Click"/>
        <Button Background="Black" Style="{StaticResource Geheimknopf}" Grid.Row="0" Grid.Column="1"  x:Name="r2" Visibility="Hidden" Click="r_Click"/>
        <Button Background="Black" Style="{StaticResource Geheimknopf}" Grid.Row="0" Grid.Column="2"  x:Name="r3" Visibility="Hidden" Click="r_Click"/>
        <Button Background="Black" Style="{StaticResource Geheimknopf}" Grid.Row="1" Grid.Column="2"  x:Name="r4" Visibility="Hidden" Click="r_Click"/>
        <Button Background="Black" Style="{StaticResource Geheimknopf}" Grid.Row="1" Grid.Column="1"  x:Name="r5" Visibility="Hidden" Click="r_Click"/>
        <Button Background="Black" Style="{StaticResource Geheimknopf}" Grid.Row="1" Grid.Column="0"  x:Name="r6" Visibility="Hidden" Click="r_Click"/>
    </Grid>
</Window>